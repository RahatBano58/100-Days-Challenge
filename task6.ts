// Store a person's name, & include some whitespace character at the beginning and end of the name.//
//Use "\t"(tab) and "\n"(new line) atleast once.Print the name once, so the whitespace around the name is displayed.//
// then print name after stripping the whitespaces//

let personName:String = "\t\n Rahat Bano \t\n";
console.log(personName);
console.log(personName.trim());
